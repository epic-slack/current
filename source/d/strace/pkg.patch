diff --git a/strace.SlackBuild b/strace.SlackBuild
index 1fcd1dd..f816d23 100644
--- a/strace.SlackBuild
+++ b/strace.SlackBuild
@@ -52,6 +52,8 @@ elif [ "$ARCH" = "s390" ]; then
   SLKCFLAGS="-O2"
 elif [ "$ARCH" = "x86_64" ]; then
   SLKCFLAGS="-O2 -march=x86-64 -mtune=generic -fPIC"
+elif [ "$ARCH" = "ia64" ]; then
+  SLKCFLAGS="-O2 -fPIC"
 else
   SLKCFLAGS="-O2"
 fi
