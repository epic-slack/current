diff --git a/mtr.SlackBuild b/mtr.SlackBuild
index f5dc35a..c98a590 100644
--- a/mtr.SlackBuild
+++ b/mtr.SlackBuild
@@ -51,6 +51,8 @@ if [ "$ARCH" = "i686" ]; then
   SLKCFLAGS="-O2 -march=pentium4 -mtune=generic"
 elif [ "$ARCH" = "x86_64" ]; then
   SLKCFLAGS="-O2 -march=x86-64 -mtune=generic -fPIC"
+elif [ "$ARCH" = "ia64" ]; then
+  SLKCFLAGS="-O2 -fPIC"
 else
   SLKCFLAGS="-O2"
 fi
