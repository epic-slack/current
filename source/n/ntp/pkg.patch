diff --git a/ntp.SlackBuild b/ntp.SlackBuild
index 8c3097a..801d971 100644
--- a/ntp.SlackBuild
+++ b/ntp.SlackBuild
@@ -52,6 +52,8 @@ elif [ "$ARCH" = "i686" ]; then
   SLKCFLAGS="-O2 -march=pentium4 -mtune=generic"
 elif [ "$ARCH" = "x86_64" ]; then
   SLKCFLAGS="-O2 -march=x86-64 -mtune=generic -fPIC"
+elif [ "$ARCH" = "ia64" ]; then
+  SLKCFLAGS="-O2 -fPIC"
 else
   SLKCFLAGS="-O2"
 fi
