diff --git a/diffutils.SlackBuild b/diffutils.SlackBuild
index 8c979d2..feff444 100644
--- a/diffutils.SlackBuild
+++ b/diffutils.SlackBuild
@@ -51,6 +51,8 @@ if [ "$ARCH" = "i686" ]; then
   SLKCFLAGS="-O2 -march=pentium4 -mtune=generic"
 elif [ "$ARCH" = "x86_64" ]; then
   SLKCFLAGS="-O2 -march=x86-64 -mtune=generic -fPIC"
+elif [ "$ARCH" = "ia64" ]; then
+  SLKCFLAGS="-O2 -fPIC"
 else
   SLKCFLAGS="-O2"
 fi
